/// <reference types="node" />
import http from 'http';
import Router from './lib/router';
import { WagaRequest, WagaValidMethod, WagaResponse } from "./types";
export default class App extends Router {
    #private;
    constructor();
    getTypeFile(path: string): string;
    setTypeFile(ext: string, type: string): this;
    listen(port: number, callback?: (port?: number) => void): http.Server<typeof http.IncomingMessage, typeof http.ServerResponse>;
    getValids(req: WagaRequest): WagaValidMethod[];
    toFunction(): (request: WagaRequest, response: WagaResponse) => Promise<void>;
}
